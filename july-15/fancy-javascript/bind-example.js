

function Person(name) {
  this.name = name
}

function TimeoutWrapper() {
  this.setTimeout = function(input) {
    setTimeout(function() {
      console.log
    }}
  }
}

Person.prototype.delayGreet = function() {
  setTimeout
}
